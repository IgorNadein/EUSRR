/**
 * @fileoverview Calendar Event Drag & Drop functionality
 * @module components/calendarEventDragDrop
 */

/**
 * Инициализация drag & drop для событий календаря
 * @param {Object} options - Опции инициализации
 * @returns {Object} API для управления drag & drop
 */
export function initCalendarEventDragDrop(options = {}) {
  console.log('[CalendarEventDragDrop] Initialized (stub)');
  
  return {
    destroy: () => {
      console.log('[CalendarEventDragDrop] Destroyed');
    }
  };
}
